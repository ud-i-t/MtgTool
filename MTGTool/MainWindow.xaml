<Window x:Class="MTGTool.MainWindow"
        Name="Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MTGTool"
        xmlns:vm="clr-namespace:MTGTool.ViewModel"
        xmlns:view="clr-namespace:MTGTool.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1600"
        WindowStyle="SingleBorderWindow">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Left" Width="200">
            <view:MessageList></view:MessageList>
            <Button Command="{Binding Path=AddCommand}">
                <TextBlock Text="追加"></TextBlock>
            </Button>
            <Button Command="{Binding Path=EditCommand}">
                <TextBlock Text="編集"></TextBlock>
            </Button>
            <Button Command="{Binding Path=GreetCommand}">
                <TextBlock Text="再生"></TextBlock>
            </Button>
        </StackPanel>
        <Grid Name="canvas" Width="1280" Height="720" HorizontalAlignment="Left" Margin="200,24,0,25">
            <Image Source="Image/UI/背景.png" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="{Binding RelativeSource={RelativeSource Self}, Path=Source.PixelWidth}" Height="{Binding RelativeSource={RelativeSource Self}, Path=Source.PixelHeight}"/>
            <Image Source="Image/chara/sakuya1.png" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="{Binding RelativeSource={RelativeSource Self}, Path=Source.PixelWidth}" Height="{Binding RelativeSource={RelativeSource Self}, Path=Source.PixelHeight}"/>
            <Image Source="Image/UI/メッセージウインドウ.png" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,-37,0,37" Width="1280"/>
            <TextBlock Text="{Binding Path=Name}" FontSize="30" FontFamily="メイリオ" Background="Transparent" Margin="28,0,0,178" HorizontalAlignment="Left" Width="{Binding RelativeSource={RelativeSource Self}, Path=Source.PixelWidth}" Height="{Binding RelativeSource={RelativeSource Self}, Path=Source.PixelHeight}" VerticalAlignment="Bottom"/>
            <view:EditMessage Margin="0,10,-427,-10" HorizontalAlignment="Right" Width="419"></view:EditMessage>
        </Grid>
    </Grid>
</Window>
