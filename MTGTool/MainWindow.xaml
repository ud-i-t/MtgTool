<Window x:Class="MTGTool.MainWindow"
        Name="Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MTGTool"
        xmlns:vm="clr-namespace:MTGTool.ViewModel"
        xmlns:view="clr-namespace:MTGTool.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <DockPanel LastChildFill="False">
        <view:EditMessage DockPanel.Dock="Right"></view:EditMessage>
        <TextBlock Text="{Binding Path=Name}" DockPanel.Dock="Right">
        </TextBlock>
        <StackPanel Width="200" DockPanel.Dock="Left">
            <view:MessageList Height="500"></view:MessageList>
            <Button>
                <TextBox Text="編集"></TextBox>
            </Button>
            <Button Command="{Binding Path=GreetCommand}" DockPanel.Dock="Right">
                <TextBox Text="再生"></TextBox>
            </Button>
        </StackPanel>
    </DockPanel>
</Window>
